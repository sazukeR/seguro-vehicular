import { createTheme } from "@mui/material";

export const theme = createTheme({
 palette: {
  primary: {
   main: "#FF1C44",
  },
  secondary: {
   main: "#FAFBFF",
  },
  primaryLight: {
   main: "#6F7DFF",
  },
 },
});
